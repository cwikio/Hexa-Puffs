"""Network tools — placeholder for Phase 3 (get_connections, send_connection_request)."""

import logging

from fastmcp import FastMCP

logger = logging.getLogger("linkedin")


def register_network_tools(mcp: FastMCP) -> None:
    """Phase 3 — network management tools will be registered here."""
    pass
