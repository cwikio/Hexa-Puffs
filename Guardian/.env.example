# Guardian MCP Server Configuration

# Transport mode: "stdio" (default, for Claude Desktop) or "http" (for HTTP/SSE)
TRANSPORT=stdio

# HTTP port (only used when TRANSPORT=http)
PORT=3000

# --- Provider Selection ---
# If GROQ_API_KEY is set, Guardian uses Groq. Otherwise it falls back to Ollama.

# Groq configuration (recommended)
GROQ_API_KEY=your-groq-api-key-here
GROQ_MODEL=meta-llama/llama-guard-4-12b

# Ollama configuration (fallback when no GROQ_API_KEY)
OLLAMA_HOST=http://localhost:11434
MODEL_NAME=guardian