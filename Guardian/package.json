{
  "name": "guardian-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for scanning content for prompt injection attacks using Granite Guardian",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:lifecycle": "vitest run lifecycle"
  },
  "keywords": [
    "mcp",
    "security",
    "prompt-injection",
    "guardian",
    "model-context-protocol"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
